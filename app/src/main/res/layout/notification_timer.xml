<?xml version="1.0" encoding="utf-8"?>
<!-- Custom collapsed/expanded content for the running-timer notification.
     Chronometer only: the collapsed content area is too short for a second
     line on several skins (the label rendered clipped, #37), so any timer
     label rides in the header subText next to "Rings at" instead. The
     Chronometer stays OS-rendered and OS-ticked (countDown mode) -- zero
     process time. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <Chronometer
        android:id="@+id/timer_chronometer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
        android:textSize="28sp"
        android:fontFamily="sans-serif-medium" />
</LinearLayout>
