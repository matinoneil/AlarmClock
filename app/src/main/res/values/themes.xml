<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.AlarmClock" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Fully transparent window for ReminderSnoozeActivity: the Compose
         dialog inside draws its own scrim and card, so the activity floats a
         small menu over whatever app is in front (Tasks-style) instead of
         showing as a screen. No window animation: the dialog should just
         appear/disappear. -->
    <style name="Theme.AlarmClock.SnoozeDialog" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
